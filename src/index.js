import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

import GifApp from './GiftApp';


ReactDOM.render(
  <React.StrictMode>
    <GifApp />
  </React.StrictMode>,
  document.getElementById('root')
);
